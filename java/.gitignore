# Save game của người dùng (mỗi user/session 1 file) - KHÔNG commit
saves/

# Phụ thuộc
node_modules/
jar
# Fallback bundle cache (sinh tự động từ jar/) - KHÔNG commit
assets_mobile/web/apps/
assets_pc/web/apps/
web/apps/
